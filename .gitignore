# Secrets / credentials — NEVER commit
config/*.env
config/*.json
config/*.key
.config/
*.pem
*.p12
.mcp.json

# Direnv
.envrc.local

# Node / TypeScript
node_modules/
dist/
build/
*.tsbuildinfo
.turbo/

# Pulumi
**/Pulumi.*.yaml.bak
.pulumi/

# Logs
*.log
logs/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
*~

# Claude Code local
.claude/cache/
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/tmp/
.claude/worktrees/

# vitest coverage output
coverage/

# generated cover image fonts (downloaded on first run, cached for reuse)
.cache/

# private operational notes (named individuals / voice rules / rejected drafts /
# strategy details — kept local only, never committed). 内容は Ryan ローカルに
# 残し、graph migrate / compact-log script は file が存在する時のみ動作する。
operations/log.md
operations/log.archive.*.md
x-account-strategy.md
