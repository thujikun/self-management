{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**", "tsconfig.json", "package.json", "vite.config.ts", "wrangler.jsonc"],
      "outputs": ["dist/**", "src/routeTree.gen.ts"]
    }
  }
}
